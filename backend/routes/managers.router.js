const express = require('express');

// const {
//   httpGetAllManagers,
//   httpGetSingleManager,
// } = require('../controllers/managers.controller');

const managersRouter = express.Router();

// managersRouter.get('/', httpGetAllPlayers);
// managersRouter.get('/:id', httpGetAllPlayers);

module.exports = managersRouter;
